# Prompt: Global i18n Pass (bf-bp5)

> Paste ke Antigravity (mode A) atau kerjakan direct. Plan lengkap: `docs/plans/2026-07-23-bf-bp5-i18n-pass.md`.
> **JANGAN mulai kode sebelum Task 0 (keputusan A vs B) beres.**

---

## Konteks wajib baca

1. `AGENTS.md` § "Language: English-first" — rule + debt yang sudah dicatat.
2. `AGENTS.md` § arsitektur Server Actions (`ServerActionResult`, `handleApiError`).
3. `src/lib/errorUtils.ts` — `ErrorContext` union + `getErrorMessage`.
4. Wishlist feature (`src/app/(app)/wishlist/`) = **contoh sudah English**, ikuti polanya.
5. Versi Next.js project ini breaking — baca `node_modules/next/dist/docs/` sebelum sentuh `next.config.ts`/`middleware.ts`. JANGAN asumsi dari training.

## Task 0 — Explore & Decide (WAJIB dulu, model Opus)

1. Scope Indonesian strings:
   ```bash
   grep -rniE '\b(memuat|menyimpan|menghapus|mengupdate|batal|tutup|pilih|wajib|tidak valid|gagal|belum ada|simpan|hapus|tambah)\b' src/app src/lib src/components --include=*.tsx --include=*.ts | grep -v wishlist
   ```
2. Cek `next-intl` kompat dengan versi Next di `package.json` + docs lokal.
3. **Putuskan A (hardcode EN) atau B (next-intl)**. Tulis keputusan + alasan di sini sebelum lanjut:

   ```
   KEPUTUSAN: [A / B]
   ALASAN: ...
   ```

## Task 1 — errorUtils

Jalankan sesuai opsi (lihat plan Task 1). Grep SEMUA call site dulu:
```bash
grep -rn 'handleApiError(' src/app --include=*.ts
```
Ubah type + semua call site dalam 1 pass biar TS nggak error.

## Task 2 — Sisa UI per fitur

Urutan: accounts → transactions → budgets → goals → assets → settings.
Per fitur: scan Indonesian → translate/key-kan. Wishlist skip (contoh).

## Task 3 — Locale files (opsi B saja)

`src/i18n/messages/en.json` + `id.json`. Nested per fitur.

## Aturan

- Ngobrol/jelasin ke user tetap Indonesian. Yang English = string DI DALAM app.
- JANGAN sentuh komentar kode / nama variabel / doc internal.
- Tiap fitur selesai → sanity build sebelum lanjut fitur berikut.

## Verifikasi akhir

```bash
grep -rniE '\b(memuat|menyimpan|batal|tutup|wajib|tidak valid|gagal)\b' src/app src/components --include=*.tsx | grep -v id.json
# → 0 hasil user-facing
npm run build   # WAJIB lolos (minta user run, jangan run sendiri)
```

## Setelah selesai
- Update `AGENTS.md` (opsi B: tambah section i18n pattern).
- `bd close bf-bp5`.
